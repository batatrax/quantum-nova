/**
 * docs/sections/shortcuts.js
 * Section "Raccourcis clavier" — visible en fin du panneau d'aide.
 */

window.QNHelp = window.QNHelp || {};

window.QNHelp.shortcuts = {
    title: 'Raccourcis',
    html: `
        <h2>Raccourcis clavier</h2>
        <p><code>Entrée</code> — calcule (mode Calcul / Scientifique) ou trace (mode Graphique)</p>
        <p><code>Échap</code> — efface la saisie courante</p>
        <p><code>⌫ Backspace</code> — efface le dernier caractère</p>

        <div class="help-todo">À compléter selon les raccourcis effectivement implémentés (ouverture menu, bascule mode, etc.).</div>
    `
};
